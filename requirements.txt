# Core dependencies. Install with: make install  (or pip install -r requirements.txt)
#
# LightGBM is intentionally excluded so `pip install -r` does not overwrite an
# existing CUDA-enabled LightGBM build. `make install` leaves installed LightGBM
# unchanged; use INSTALL_LIGHTGBM=cpu for CI/CPU-only environments.
pandas==2.2.2
numpy==1.26.4
catboost==1.2.7
xgboost==2.1.2
optuna==4.0.0
shap==0.46.0
scikit-learn==1.5.1
matplotlib==3.9.2
fastapi==0.115.5
uvicorn==0.32.0
locust==2.32.1
httpx==0.27.2
PyYAML==6.0.2
pytest==8.3.3
pytest-cov==6.0.0
ruff==0.8.6
black==24.10.0
isort==5.13.2
mypy==1.13.0
